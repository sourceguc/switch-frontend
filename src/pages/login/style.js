import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  loginContainer: {
    gap: "5em",
  },
  buttonContainer: {
    gap: "1em",
  },
  title: {
    fontFamily: "Source",
    fontWeight: 400,
    FontSize: "34px",
  },
  textContainer: {
    textAlign: "center",
  },
  paragraph: {
    fontSize: "12px",
    marginTop: "-1em"
  }
});

export default useStyles;
